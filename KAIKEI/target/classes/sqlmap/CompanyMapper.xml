<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ts.kaikei.dao.CompanyDAO">

	<select id="checkCode" parameterType="String" resultType="int" >
    
        SELECT count(*)
        FROM company
        WHERE 1=1
        AND company_cd = #{code}
        
    </select>

	<insert id="signUpCompany" parameterType="CompanyRegistVO" >
    
        INSERT INTO company(company_cd, auth_cd, title, reg_cd, fax, phone, 
        					domain, ent_id, ent_date, ent_prog, mod_id, mod_date, mod_prog)
        VALUES(
        #{company_cd}, #{auth_cd}, #{company_title}, #{company_regist_cd}, #{company_fax}, #{company_phone}, 
        #{company_domain}, #{ent_id}, NOW(), #{ent_prog}, #{mod_id}, NOW(), #{mod_prog})
        
	</insert>

</mapper>