<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ts.kaikei.dao.CompanyDAO">

	<select id="checkCode" parameterType="String" resultType="int" >
    
        SELECT count(*)
        FROM users
        WHERE 1=1
        AND id = #{id}
        
    </select>

	<insert id="signUpCompany" parameterType="CompanyVO" >
    
        INSERT INTO company
        VALUES(
        #{company_cd}, #{state_cd}, #{title}, 
        #{regist_cd}, #{fax}, #{phone}, 
        #{domain}, #{bank_title}, #{bank_acc}, 
        #{ent_id}, NOW(), #{ent_prog},
        #{mod_id}, NOW(), #{mod_prog})
        
	</insert>
    
</mapper>