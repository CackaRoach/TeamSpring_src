<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ts.kaikei.dao.StatementDAO">
	
    <select id="getStatementList" resultType="StatementVO">
    
	    SELECT company_cd, seq, account_cd, customer_cd, date, classify, debtor, creditor, abs, ent_id, ent_date, ent_prog, mod_id, mod_date, mod_prog
	    FROM statements
	    ORDER BY seq ASC
	    
    </select>
    
    <select id="getStatementListCond" parameterType="String" resultType="StatementVO">
    
    	SELECT SUM(debtor) AS qDeb, SUM(creditor) AS qCre
		FROM statements
		WHERE 1=1
		AND company_cd = #{company_cd}
		AND date between #{date1} AND #{date2}
		
    </select>

    <select id="getStatements" parameterType="String" resultType="StatementVO">
    	select *
    	from statements
    	where company_cd = #{company_cd}
    </select>
    
</mapper>